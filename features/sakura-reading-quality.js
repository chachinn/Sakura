/* Sakura Reading Garden Quality Shelf v1.2
   V1-ready curation: 50 substantial Articles + 50 public-domain Stories.
   Larger source archives stay intact; future material types remain a 50-item roadmap. */
(function(){
'use strict';
if(window.SakuraReadingQuality)return;
const TARGET=50,LEVELS=['N5','N4','N3','N2','N1'];
const TOPICS=['beauty','food','travel','digital','consumer','health','environment','culture','work','society'];
const STORY_CATEGORIES=['classics','modern-literature','mystery-suspense','children-stories','human-bonds','strange-horror'];
const STORY_QUOTAS=Object.freeze({classics:9,'modern-literature':9,'mystery-suspense':8,'children-stories':8,'human-bonds':8,'strange-horror':8});
const PREFS_KEY='sakuraReadingGardenPrefsV3',LIBRARY_KEY='sakuraReadingGardenLibraryV2';
let allArticles=[],articleShelf=[],allStories=[],storyShelf=[],currentArticleView=[],currentStoryView=[],renderTimer=0,observer=null,ready=false;
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
const setText=(node,value)=>{const text=String(value??'');if(node&&node.textContent!==text)node.textContent=text};
function read(key,fallback){try{return JSON.parse(localStorage.getItem(key)||'null')||fallback}catch{return fallback}}
function prefs(){return read(PREFS_KEY,{level:'all',mode:'furigana',articleTopic:'all',storyCategory:'all'})}
function library(){return read(LIBRARY_KEY,{saved:[],completed:[],lastReadingId:'',lastArticleId:'',lastStoryId:'',lastReadingType:''})}
function savedSet(){return new Set(Array.isArray(library().saved)?library().saved:[])}
function completedSet(){return new Set(Array.isArray(library().completed)?library().completed:[])}
function rankArticle(a,b){return (+b.estimatedMinutes||0)-(+a.estimatedMinutes||0)||String(b.sourcePublishedDate||'').localeCompare(String(a.sourcePublishedDate||''))||String(b.summary||'').length-String(a.summary||'').length||String(a.id).localeCompare(String(b.id))}
function buildArticles(records){const groups=new Map();records.forEach(x=>{const k=`${x.jlpt}:${x.topic}`;if(!groups.has(k))groups.set(k,[]);groups.get(k).push(x)});const out=[];LEVELS.forEach(level=>TOPICS.forEach(topic=>{const group=groups.get(`${level}:${topic}`)||[];if(group.length)out.push([...group].sort(rankArticle)[0])}));if(out.length<TARGET){const used=new Set(out.map(x=>x.id));[...records].sort(rankArticle).forEach(x=>{if(out.length<TARGET&&!used.has(x.id)){used.add(x.id);out.push(x)}})}return out.slice(0,TARGET)}
function rankStory(a,b){return String(b.originalExcerpt||'').length-String(a.originalExcerpt||'').length||(+b.estimatedMinutes||0)-(+a.estimatedMinutes||0)||String(a.title||'').localeCompare(String(b.title||''),'ja')}
function buildStories(records){const out=[],used=new Set();STORY_CATEGORIES.forEach(category=>{const quota=STORY_QUOTAS[category]||8;records.filter(x=>x.category===category).sort(rankStory).slice(0,quota).forEach(x=>{if(!used.has(x.id)){used.add(x.id);out.push(x)}})});if(out.length<TARGET)[...records].sort(rankStory).forEach(x=>{if(out.length<TARGET&&!used.has(x.id)){used.add(x.id);out.push(x)}});return out.slice(0,TARGET)}
function patchMaterials(){const rg=window.SakuraReadingGarden;if(!rg?.materials)return;rg.materials.forEach(item=>{item.count=TARGET;if(item.id==='articles')item.status='50 curated · long-form ready';else if(item.id==='short-stories')item.status='50 curated · extended on open';else if(item.id==='manga')item.status='Coming later · licensing first';else item.status='Coming later · 50 max'})}
function organizeMaterials(dialog){const grid=dialog.querySelector('.reading-garden-material-grid');if(!grid||dialog.querySelector('#sakura-reading-coming-later'))return;const future=[...grid.querySelectorAll('[data-reading-material]')].filter(b=>!['articles','short-stories'].includes(b.dataset.readingMaterial));if(!future.length)return;const details=document.createElement('details');details.id='sakura-reading-coming-later';details.className='sakura-reading-coming-later';const summary=document.createElement('summary');summary.textContent=`Coming later · ${future.length} future shelves`;const inner=document.createElement('div');inner.className='reading-garden-material-grid sakura-reading-coming-grid';future.forEach(b=>inner.appendChild(b));details.append(summary,inner);grid.after(details)}
function patchHome(){const dialog=document.getElementById('reading-garden-dialog');if(!dialog)return;organizeMaterials(dialog);const stats=dialog.querySelectorAll('.reading-garden-stat strong'),labels=dialog.querySelectorAll('.reading-garden-stat small');setText(stats[0],'2');setText(stats[1],'100');setText(stats[2],'50 + 50');setText(labels[0],'v1 ready shelves');setText(labels[1],'curated readings ready');setText(labels[2],'Articles + Stories');const heading=[...dialog.querySelectorAll('.reading-garden-section-heading')].find(x=>x.textContent.includes('Browse by material'));setText(heading?.lastElementChild,'100 ready now');dialog.querySelectorAll('[data-reading-material]').forEach(button=>{const id=button.dataset.readingMaterial,item=window.SakuraReadingGarden?.materials?.find(x=>x.id===id);if(!item)return;setText(button.querySelector('.reading-material-main small'),`${TARGET} ${item.unit}`);setText(button.querySelector('.reading-material-status'),item.status)});setText(document.querySelector('[data-open-reading-garden] p'),'50 substantial Articles + 50 extended public-domain Short Stories ready.');const selection=document.getElementById('reading-garden-selection');if(selection){const selected=dialog.querySelector('[data-reading-material].active')?.dataset.readingMaterial,item=window.SakuraReadingGarden?.materials?.find(x=>x.id===selected);if(item)setText(selection.querySelector('strong'),`${item.title} · ${TARGET} ${item.unit}`);const small=selection.querySelector('small');if(selected==='articles')setText(small,'Ready now: 50 substantial source-grounded Articles, balanced across all 5 JLPT study levels and 10 topics. The 300-record verified source archive is retained for Saved/Continue compatibility.');else if(selected==='short-stories')setText(small,'Ready now: 50 public-domain Japanese Stories across six shelves. Opening a Story loads a longer original Aozora passage when available and caches it on this device; the bundled excerpt remains the safe fallback.');else if(selected==='manga')setText(small,'Coming later. Sakura will only add manga after legitimate redistribution and educational-use rights are verified.');else if(item)setText(small,'Coming later. This shelf follows Sakura’s quality-first rule: up to 50 substantial readings rather than hundreds of filler items.')}}
function dateText(x){return x.sourcePublishedDate?`${x.sourceDateLabel||'Published'} ${x.sourcePublishedDate}`:(x.sourceDateLabel||'Source date available')}
function articleMatches(x,p,q,topic,savedOnly,saved){if(p.level!=='all'&&x.jlpt!==p.level)return false;if(topic!=='all'&&x.topic!==topic)return false;if(savedOnly&&!saved.has(x.id))return false;if(!q)return true;return `${x.title} ${x.titleKana} ${x.titleEnglish} ${x.summary} ${x.sourcePublisher}`.toLowerCase().includes(q.toLowerCase())}
function storyMatches(x,p,q,category,savedOnly,saved){if(p.level!=='all'&&x.studyDifficulty!==p.level)return false;if(category!=='all'&&x.category!==category)return false;if(savedOnly&&!saved.has(x.id))return false;if(!q)return true;return `${x.title} ${x.titleKana} ${x.author} ${x.authorKana} ${(x.tags||[]).join(' ')}`.toLowerCase().includes(q.toLowerCase())}
function articleCard(x,saved,completed,p){const title=p.mode==='furigana'?x.titleFurigana:esc(p.mode==='kana'?x.titleKana:x.title);return `<div class="reading-article-card sakura-quality-reading" data-reading-open-article="${esc(x.id)}" role="button" tabindex="0" aria-label="Open ${esc(x.titleEnglish)}"><div><div class="reading-article-tags"><span class="reading-article-tag sakura-quality-badge">Quality Shelf</span><span class="reading-article-tag">Long-form</span><span class="reading-article-tag">Study ${esc(x.jlpt)}</span><span class="reading-article-tag">${x.topicIcon} ${esc(x.topicLabel)}</span><span class="reading-article-tag">${esc(String(x.sourceYear))} source</span>${completed.has(x.id)?'<span class="reading-article-tag">✓ Read</span>':''}</div><h3>${title}</h3><span class="reading-en-title">${esc(x.titleEnglish)}</span><p>${esc(x.summary)}</p><small class="reading-source-inline">Source: ${esc(x.sourcePublisher)} · ${esc(dateText(x))}</small></div><button class="reading-article-save${saved.has(x.id)?' saved':''}" type="button" data-reading-save-article="${esc(x.id)}" aria-label="${saved.has(x.id)?'Remove from Saved':'Save Article'}">${saved.has(x.id)?'♥':'♡'}</button></div>`}
function storyCard(x,saved,completed){const year=x.sourceYear?String(x.sourceYear):'Year not listed';return `<div class="reading-article-card sakura-quality-reading" data-reading-open-story="${esc(x.id)}" role="button" tabindex="0" aria-label="Open ${esc(x.title)}"><div><div class="reading-article-tags"><span class="reading-article-tag sakura-quality-badge">Quality Shelf</span><span class="reading-article-tag">Extended on open</span><span class="reading-article-tag">Approx ${esc(x.studyDifficulty)}</span><span class="reading-article-tag">${esc(x.categoryIcon||'📚')} ${esc(x.categoryLabel||x.category)}</span><span class="reading-article-tag">${esc(year)}</span>${completed.has(x.id)?'<span class="reading-article-tag">✓ Read</span>':''}</div><h3>${esc(x.title)}</h3><span class="reading-en-title">${esc(x.titleKana)} · ${esc(x.author)}</span><p>${esc(x.originalExcerpt)}</p><small class="reading-source-inline">Source: 青空文庫 · ${esc(year)} · public-domain original</small></div><button class="reading-article-save${saved.has(x.id)?' saved':''}" type="button" data-reading-save-story="${esc(x.id)}" aria-label="${saved.has(x.id)?'Remove from Saved':'Save Story'}">${saved.has(x.id)?'♥':'♡'}</button></div>`}
function renderArticles(){const browser=document.getElementById('reading-articles-browser'),list=document.getElementById('reading-article-list'),count=document.getElementById('reading-article-count');if(!browser||browser.hidden||!list||!count||!articleShelf.length)return;const p=prefs(),q=document.getElementById('reading-article-search')?.value.trim()||'',topic=document.getElementById('reading-article-topic')?.value||p.articleTopic||'all',savedOnly=browser.dataset.savedOnly==='true',saved=savedSet(),completed=completedSet(),base=savedOnly?allArticles:articleShelf;currentArticleView=base.filter(x=>articleMatches(x,p,q,topic,savedOnly,saved));const sig=`a:${currentArticleView.map(x=>x.id).join('|')}:${[...saved].join('|')}:${[...completed].join('|')}:${p.mode}`;if(list.dataset.qualitySignature!==sig){list.dataset.qualitySignature=sig;list.innerHTML=currentArticleView.length?currentArticleView.map(x=>articleCard(x,saved,completed,p)).join(''):'<div class="reading-browser-empty">🌸 No quality-shelf Articles match these filters.</div>'}setText(count,`${currentArticleView.length.toLocaleString()} curated article${currentArticleView.length===1?'':'s'}`);const more=document.getElementById('reading-load-more');if(more&&!more.hidden)more.hidden=true;setText(browser.querySelector('.reading-browser-hero p'),'50 substantial source-grounded Articles are ready: one curated reading for every JLPT level × topic combination. Opening an Article builds its multi-section feature lazily, while the 300-record verified archive remains intact.');const offline=document.getElementById('reading-offline-status');if(offline)setText(offline,'The visible shelf is capped at 50; offline preparation keeps the supporting source-archive packs so Saved and Continue readings are never stranded.')}
function renderStories(){const browser=document.getElementById('reading-stories-browser'),list=document.getElementById('reading-story-list'),count=document.getElementById('reading-story-count');if(!browser||browser.hidden||!list||!count||!storyShelf.length)return;const p=prefs(),q=document.getElementById('reading-story-search')?.value.trim()||'',category=document.getElementById('reading-story-category')?.value||p.storyCategory||'all',savedOnly=browser.dataset.savedOnly==='true',saved=savedSet(),completed=completedSet(),base=savedOnly?allStories:storyShelf;currentStoryView=base.filter(x=>storyMatches(x,p,q,category,savedOnly,saved));const sig=`s:${currentStoryView.map(x=>x.id).join('|')}:${[...saved].join('|')}:${[...completed].join('|')}`;if(list.dataset.qualitySignature!==sig){list.dataset.qualitySignature=sig;list.innerHTML=currentStoryView.length?currentStoryView.map(x=>storyCard(x,saved,completed)).join(''):'<div class="reading-browser-empty">🌸 No quality-shelf Short Stories match these filters.</div>'}setText(count,`${currentStoryView.length.toLocaleString()} curated stor${currentStoryView.length===1?'y':'ies'}`);const more=document.getElementById('reading-story-load-more');if(more&&!more.hidden)more.hidden=true;setText(browser.querySelector('.reading-browser-hero p'),'50 curated public-domain Japanese works are ready across six shelves. Open a Story to load a longer original passage from its Aozora source; Sakura caches successful passages on this device and keeps the bundled short excerpt as a fallback.');const offline=document.getElementById('reading-story-offline-status');if(offline)setText(offline,'The bundled Story pack is the offline fallback. After a longer original passage loads successfully, Sakura also keeps that displayed passage on this device for rereading.')}
function reconcile(){patchHome();renderArticles();renderStories()}
function schedule(){clearTimeout(renderTimer);renderTimer=setTimeout(reconcile,0)}
function syntheticOpen(id,type){const dialog=document.getElementById('reading-garden-dialog');if(!dialog||!id)return;const b=document.createElement('button');b.type='button';b.hidden=true;if(type==='story')b.dataset.readingOpenStory=id;else b.dataset.readingOpenArticle=id;dialog.appendChild(b);b.click();setTimeout(()=>b.remove(),0)}
function randomFrom(type){const p=prefs(),pool=(type==='story'?storyShelf:articleShelf).filter(x=>p.level==='all'||(type==='story'?x.studyDifficulty:x.jlpt)===p.level);return pool[Math.floor(Math.random()*pool.length)]}
function moveCurated(type,direction){const lib=library(),id=lib.lastReadingId||lib.lastArticleId||lib.lastStoryId,pool=type==='story'?currentStoryView:currentArticleView;if(!pool.length)return;const i=pool.findIndex(x=>x.id===id);if(i<0)return;const next=Math.max(0,Math.min(pool.length-1,i+direction));if(next!==i)syntheticOpen(pool[next].id,type)}
function bind(){document.addEventListener('click',e=>{if(e.target.closest('[data-reading-load-more],[data-reading-load-more-stories]')){e.preventDefault();e.stopImmediatePropagation();schedule();return}if(e.target.closest('[data-reading-surprise-article]')&&articleShelf.length){e.preventDefault();e.stopImmediatePropagation();const x=randomFrom('article');if(x)syntheticOpen(x.id,'article');return}if(e.target.closest('[data-reading-surprise-story]')&&storyShelf.length){e.preventDefault();e.stopImmediatePropagation();const x=randomFrom('story');if(x)syntheticOpen(x.id,'story');return}if(e.target.closest('[data-reading-prev-article]')){e.preventDefault();e.stopImmediatePropagation();moveCurated('article',-1);return}if(e.target.closest('[data-reading-next-article]')){e.preventDefault();e.stopImmediatePropagation();moveCurated('article',1);return}if(e.target.closest('[data-reading-prev-story]')){e.preventDefault();e.stopImmediatePropagation();moveCurated('story',-1);return}if(e.target.closest('[data-reading-next-story]')){e.preventDefault();e.stopImmediatePropagation();moveCurated('story',1);return}schedule()},true);document.addEventListener('input',e=>{if(['reading-article-search','reading-story-search'].includes(e.target?.id))schedule()},true);document.addEventListener('change',e=>{if(['reading-article-topic','reading-story-category'].includes(e.target?.id))schedule()},true);const dialog=document.getElementById('reading-garden-dialog');if(dialog){observer=new MutationObserver(schedule);observer.observe(dialog,{subtree:true,childList:true,attributes:true,attributeFilter:['hidden','class']})}}
function style(){if(document.getElementById('sakura-reading-quality-style'))return;const s=document.createElement('style');s.id='sakura-reading-quality-style';s.textContent='.sakura-quality-badge{border-color:color-mix(in srgb,var(--color-primary) 38%,var(--color-border))!important;background:color-mix(in srgb,var(--color-primary-soft) 82%,var(--color-surface))!important;color:var(--color-primary-dark)!important;font-weight:900!important}.sakura-quality-reading{content-visibility:auto;contain-intrinsic-size:150px}.sakura-reading-coming-later{margin-top:10px;padding:8px 10px;border:1px dashed var(--color-border);border-radius:13px}.sakura-reading-coming-later>summary{cursor:pointer;color:var(--color-text-muted);font-size:9px;font-weight:850}.sakura-reading-coming-grid{margin-top:10px}';document.head.appendChild(s)}
async function init(){if(ready)return;const rg=window.SakuraReadingGarden;if(!rg?.loadArticleIndexes||!rg?.loadStories){setTimeout(init,120);return}ready=true;style();patchMaterials();bind();patchHome();try{const [articles,stories]=await Promise.all([rg.loadArticleIndexes('all'),rg.loadStories()]);allArticles=articles;allStories=stories;articleShelf=buildArticles(articles);storyShelf=buildStories(stories);if(articleShelf.length!==TARGET)console.warn(`Reading Quality Shelf expected ${TARGET} Articles, got ${articleShelf.length}.`);if(storyShelf.length!==TARGET)console.warn(`Reading Quality Shelf expected ${TARGET} Stories, got ${storyShelf.length}.`);reconcile()}catch(error){console.warn('Reading Quality Shelf could not finish curation; Reading Garden core remains available.',error)}}
window.SakuraReadingQuality=Object.freeze({version:1.2,target:TARGET,init,get articleCount(){return articleShelf.length},get storyCount(){return storyShelf.length}});init();
}());
